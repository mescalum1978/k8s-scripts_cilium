# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5).

source /etc/network/interfaces.d/*

# The loopback network interface
auto lo
iface lo inet loopback

# The primary network interface
allow-hotplug ens18
iface ens18 inet static
  address 192.168.1.24/24
  gateway 192.168.1.1
  broadcast 192.168.1.255

  # Static route to worker pod network
  up ip route add 10.244.1.0/24 via 192.168.1.25 dev ens18
  down ip route del 10.244.1.0/24 via 192.168.1.25 dev ens18




# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5).

source /etc/network/interfaces.d/*

# The loopback network interface
auto lo
iface lo inet loopback

# The primary network interface
allow-hotplug ens18
iface ens18 inet static
	address 192.168.1.25/24
	gateway 192.168.1.1
	broadcast 192.168.1.255

  # Static route to master pod network
  up ip route add 10.244.0.0/24 via 192.168.1.24 dev ens18
  down ip route del 10.244.0.0/24 via 192.168.1.24 dev ens18
